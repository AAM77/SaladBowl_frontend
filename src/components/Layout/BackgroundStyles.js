import styled from 'styled-components';

export const Styles = styled.div`

  #background {
    position: fixed;
    top: 0;
    left: 0;
    z-index: -4;
    opacity: 0.7;

    /* Preserve aspect ratio */
    width: 100%;
    height: 100%;
  }
`;
